#include<stdio.h>
#include<math.h>
int main(){
    int p;
    printf("enter the number is :");
    scanf("%d",&p);
    int root = sqrt(p);
    printf("the root of number is :%d",root);
    return 0;
}