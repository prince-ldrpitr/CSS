#include<stdio.h>
#include<math.h>
int main(){
    int u;
    printf("enter the number is :");
    scanf("%d",&u);
    int root = sqrt(u);
    printf("the root of number is :%d",root);
    return 0;
}