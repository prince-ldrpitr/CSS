#include<stdio.h>
#include<math.h>
int main(){
    int l;
    printf("enter the number is :");
    scanf("%d",&l);
    int root = sqrt(l);
    printf("the root of number is :%d",root);
    return 0;
}